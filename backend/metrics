app.get('/metrics', async (req, res) => {
  res.set('Content-Type', 'text/plain');
  res.send(`# HELP ul_train_signal_total Total signals tracked\nul_train_signal_total ${await Signal.countDocuments()}`);
});
